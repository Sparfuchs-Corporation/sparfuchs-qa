# Cross-Model QA System

Auto-complete: **{{AUTO_COMPLETE_STATUS}}**.

All operations are restricted to read-only analysis of the local repository unless
auto-complete is explicitly enabled. No destructive commands, no network writes,
no git pushes, and no external API calls unless explicitly permitted.

Behavior may differ between models. Follow all injected instructions precisely.
